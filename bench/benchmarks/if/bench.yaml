---
name: if
description: >-
  Measure synthetic if/else dispatch over repeated boolean events.
tags:
  operators: if
inputs:
  main:
    path: empty.ndjson
    source:
      num_events: 200000
env:
  TENZIR_CONSOLE_FORMAT: none
runtime:
  warmup_runs: 1
  measurement_runs: 3
  timeout_seconds: 900
