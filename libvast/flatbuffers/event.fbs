include "data_table.fbs";

namespace vast.detail;

table Attribute {
  key:string (key);
  value:Data;
}

table MetaData {
  id:ulong = -1;
  timestamp:ulong = 0;
  attributes:[Attribute];
}

table Event {
  data:Data;
  meta:MetaData;
}

root_type Event;
