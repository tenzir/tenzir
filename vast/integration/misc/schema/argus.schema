type timestamp = time
type argus.argus = record{
  StartTime: timestamp,
  Flgs: string,
  Proto: string,
  SrcAddr: addr,
  Sport: count,
  Dir: string,
  DstAddr: addr,
  Dport: count,
  TotPkts: count,
  TotBytes: count,
  State: string,
  TcpRtt: real
}
