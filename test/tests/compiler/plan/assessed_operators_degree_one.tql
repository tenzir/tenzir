// parallelism: 1
// These independent transforms remain single-instance at degree one.
from {host: "localhost"}
dns_lookup host
context_enrich "hosts", key=host
python ""
each parallel=2 {
  from $this
}
