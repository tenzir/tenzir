// parallelism: 1, fuse=all
// The `fuse=all` option fuses all operator-to-operator channels,
// regardless of parallelism matching. The external `{input}` and `{output}`
// edges are never fused.
from {x: 1}
where x > 0
summarize x, n=count()
