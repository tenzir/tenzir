let $allow = "accept"
let $deny = "deny"

from {action: "accept"}, {action: "deny"}, {action: "other"}
match action {
  $allow => {
    verdict = "allowed"
  }
  $deny => {
    verdict = "blocked"
  }
  _ => {
    verdict = "unknown"
  }
}
sort action
