// The trailing `discard` makes the merged path unordered, which must not
// delete the `sort` inside the arm that ends in a sink.
from {x: 3}, {x: 1}, {x: 2}
match x {
  1 | 2 | 3 => {
    sort x
    to_stdout
  }
  _ => {
    pass
  }
}
discard
