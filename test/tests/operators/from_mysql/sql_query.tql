// Execute a custom SQL query
from_mysql sql="SELECT name, age FROM users WHERE is_active = 1 ORDER BY name",
           host=env("MYSQL_HOST"),
           port=int(env("MYSQL_PORT")),
           user=env("MYSQL_USER"),
           password=env("MYSQL_PASSWORD"),
           database=env("MYSQL_DATABASE")
