// Same as concatenated_streams, but feeding one byte at a time to exercise the
// incremental buffering across stream boundaries.
from_file f"{env("TENZIR_INPUTS")}/feather/concatenated_streams.feather" {
  split_bytes 1
  read_feather
}
