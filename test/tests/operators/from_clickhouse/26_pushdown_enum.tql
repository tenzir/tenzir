// Enum names compare by name in both TQL and ClickHouse, so this goes into SQL.
from_clickhouse table="fc_pushdown",
                host=env("CLICKHOUSE_HOST"),
                port=int(env("CLICKHOUSE_PORT")),
                password=env("CLICKHOUSE_PASSWORD"),
                tls=false
where status == "high"
select id, status
