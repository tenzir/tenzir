// With user-provided SQL, the filter and limit are applied locally.
from_clickhouse sql="SELECT id, x FROM fc_pushdown ORDER BY id",
                host=env("CLICKHOUSE_HOST"),
                port=int(env("CLICKHOUSE_PORT")),
                password=env("CLICKHOUSE_PASSWORD"),
                tls=false
where x > 0
head 2
