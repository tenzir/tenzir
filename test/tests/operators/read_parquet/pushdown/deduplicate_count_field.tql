// `deduplicate` assigns its count field itself, so the reader never decodes
// the corrupted input column of the same name.
from_file f"{env("READ_PUSHDOWN_ROOT")}/corrupt-unused" {
  read_parquet
}
deduplicate id, count_field=unused
where id < 2
select id, unused
