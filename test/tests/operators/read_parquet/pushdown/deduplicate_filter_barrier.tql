// A filter behind `deduplicate` must not move upstream: only the first event of
// each key survives, so no event with `id == 3` remains.
from_file f"{env("READ_PUSHDOWN_ROOT")}/clean" {
  read_parquet
}
deduplicate nested.sibling
where id == 3
select id
