// A downstream limit bounds the input through a strided `slice`: the third
// output is input 8128, so the corrupted second row group is never decoded.
from_file f"{env("READ_PUSHDOWN_ROOT")}/corrupt-lookahead" {
  read_parquet
}
slice begin=8000, stride=64
select id
head 3
