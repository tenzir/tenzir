---
fixtures: [http]
timeout: 60
---

from {
  u: env("HTTP_FIXTURE_METHOD_URL"),
  m: "put",
}
http u, method=m, metadata_field=meta {
  read_json
}
meta.headers.Server = replace_regex(
  meta.headers.Server,
  "Python/[0-9.]+",
  "Python/*.*.*"
)
meta.headers.Date = "*"
