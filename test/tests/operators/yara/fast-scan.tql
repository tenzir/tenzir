from_stdin {
  yara rules=r#"
rule many {
  strings:
    $a = "aa"
  condition:
    $a
}
"#, fast_scan=true
}
drop time, metadata.uid, finding_info.uid, finding_info.created_time,
     policy.uid, finding_info.analytic.uid
