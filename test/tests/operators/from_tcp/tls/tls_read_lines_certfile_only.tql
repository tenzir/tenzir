---
skip: flaky crash on macOS arm64 in from_tcp TLS shutdown path (tracked for follow-up redesign)
---

from_tcp env("TCP_ENDPOINT"),
         tls={certfile: env("TCP_CERTKEYFILE"),
              skip_peer_verification: true} { read_lines }
| head 1
