// Decapsulate VLAN information. Manually verified with:
// tshark -r vlan-single-tagging.pcap -T fields -e vlan.id
from_file f"{env("TENZIR_INPUTS")}/pcap/vlan-single-tagging.pcap" {
  read_pcap
}
this = decapsulate(this)
select vlan.outer
