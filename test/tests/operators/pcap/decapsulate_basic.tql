// Make sure basic decapsulation logic works.
from_file f"{env("TENZIR_INPUTS")}/pcap/example.pcap.gz" {
  decompress_gzip
  read_pcap
}
this = decapsulate(this)
to_stdout {
  write_tql compact=true
}
