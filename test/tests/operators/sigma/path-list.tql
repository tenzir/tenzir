// `path` accepts a list of files and directories; files reachable through
// overlapping entries are deduplicated, and discovery is deterministic.
from {id: "alpha", foo: "needle"}
sigma path=["tests/operators/sigma/inputs/basic-rule.yaml", "tests/operators/sigma/inputs/basic-rule.yaml"]
select id = evidences[0].data.id
