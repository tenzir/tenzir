warning: sigma operator skips rule 'tests/operators/sigma/inputs/families/windows-system/rules/service-type.yml' for OCSF input
 --> tests/operators/sigma/families/windows-system.tql:7:12
  |
7 | sigma path=f"{env("TENZIR_INPUTS")}/families/windows-system/rules", format="plain"
  |            ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  |
  = note: unresolved Sigma fields: `ServiceType`
  = note: field `ServiceType` is absent from the OCSF schema
  = note: use `mapping="direct"` only when the rule fields exist literally
{
  rule: "PowerShell Event Log Cleared",
  id: "log-cleared",
}
{
  rule: "Service Installed Running As LocalSystem",
  id: "match",
}
{
  rule: "Service Installed With PowerShell Command",
  id: "match",
}
{
  rule: "Windows Defender Service Stopped",
  id: "defender-stopped",
}
