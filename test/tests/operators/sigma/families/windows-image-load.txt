warning: sigma operator skips rule 'tests/operators/sigma/inputs/families/windows-image-load/rules/unsigned.yml' for OCSF input
 --> tests/operators/sigma/families/windows-image-load.tql:7:12
  |
7 | sigma path=f"{env("TENZIR_INPUTS")}/families/windows-image-load/rules", format="plain"
  |            ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  |
  = note: unresolved Sigma fields: `Signed`
  = note: field `Signed` is unmapped: OCSF encodes signing as the presence of the signature object, not as a flag.
  = note: use `mapping="direct"` only when the rule fields exist literally
{
  rule: "Validly Signed Microsoft Module",
  id: "match",
}
{
  rule: "Vault Client Loaded By Unusual Process",
  id: "expired",
}
{
  rule: "Vault Client Loaded By Unusual Process",
  id: "match",
}
