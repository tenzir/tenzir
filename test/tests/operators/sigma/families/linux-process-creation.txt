warning: sigma operator skips rule 'tests/operators/sigma/inputs/families/linux-process-creation/rules/logon-session.yml' for OCSF input
 --> tests/operators/sigma/families/linux-process-creation.tql:7:12
  |
7 | sigma path=f"{env("TENZIR_INPUTS")}/families/linux-process-creation/rules", format="plain"
  |            ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  |
  = note: unresolved Sigma fields: `LogonId`
  = note: field `LogonId` is unmapped: The Process Activity mapping does not preserve the Sysmon logon session identifier.
  = note: use `mapping="direct"` only when the rule fields exist literally
{
  rule: "Curl Piped To Shell",
  id: "match",
}
{
  rule: "Curl Piped To Shell",
  id: "unknown-os",
}
