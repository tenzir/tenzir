warning: sigma operator skips rule 'tests/operators/sigma/inputs/families/azure-activitylogs/rules/category.yml' for OCSF input
 --> tests/operators/sigma/families/azure-activitylogs.tql:7:12
  |
7 | sigma path=f"{env("TENZIR_INPUTS")}/families/azure-activitylogs/rules", format="plain"
  |            ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  |
  = note: unresolved Sigma fields: `CategoryValue`
  = note: field `CategoryValue` is unmapped: The API Activity mapping does not preserve the activity log category.
  = note: use `mapping="direct"` only when the rule fields exist literally
{
  rule: "Network Security Group Deleted",
  id: "match",
}
