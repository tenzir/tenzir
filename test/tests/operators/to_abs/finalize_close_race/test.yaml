suite: to-abs-finalize-close-race
fixtures: [abs_slow_proxy]
requires:
  operators:
    - tql2.to_azure_blob_storage
    - from_azure_blob_storage
timeout: 240
skip:
  on:
    - fixture-unavailable
    - capability-unavailable
