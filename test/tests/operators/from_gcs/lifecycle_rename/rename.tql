from_google_cloud_storage "gs://" + env("GCS_BUCKET") + "/lifecycle/rename-target.json?endpoint_override=" + env("GCS_ENDPOINT") + "&scheme=http",
         anonymous=true,
         rename=path => path + ".done" {
  read_json
}
