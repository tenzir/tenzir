suite: from-gcs
fixtures: [gcs]
requires:
  operators:
    - tql2.from_google_cloud_storage
assertions:
  fixtures:
    gcs:
      state: unchanged
timeout: 30
skip:
  on:
    - fixture-unavailable
    - capability-unavailable
