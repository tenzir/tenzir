from_google_cloud_storage "gs://" + env("GCS_BUCKET") + "/lifecycle/remove-target.json?endpoint_override=" + env("GCS_ENDPOINT") + "&scheme=http",
         anonymous=true,
         remove=true {
  read_json
}
