// Stop after the first message and NAK any prefetched messages.
from_nats env("NATS_SUBJECT"),
          url=env("NATS_URL"),
          durable=env("NATS_DURABLE"),
          count=1,
          _batch_size=2,
          _queue_capacity=2
select line = string(message)
