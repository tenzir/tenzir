from_nats env("NATS_SUBJECT"),
          url=env("NATS_URL"),
          durable=env("NATS_DURABLE"),
          count=1,
          metadata_field=nats
line = string(message)
group nats.subject {
  subject = nats.subject
}
select line, subject
