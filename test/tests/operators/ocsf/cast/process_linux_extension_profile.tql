from {
  metadata: {
    version: "1.5.0",
    extensions: [{name: "linux"}],
    profiles: ["linux/linux_users"],
  },
  class_uid: 1007,
  process: { egid: 0 }
}
ocsf::cast null_fill=true
// TODO: Re-enable once `assert` is ported to the new executor.
// assert process.has("egid")
// assert process.egid == 0
