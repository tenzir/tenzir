// Verify captured WriteLogEntries requests from the mock gRPC server.
// Count total entries across all batches and check the log_name.
from_file env("GCL_CAPTURE_FILE") { read_json }
entries = entries.length()
summarize log_name=log_name,
          total_entries=sum(entries)
