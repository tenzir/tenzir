// Verify total entries = 10000 and no batch exceeds 100 entries.
from_file env("GCL_CAPTURE_FILE") { read_json }
entries = entries.length()
summarize total_entries=sum(entries),
          num_batches=count(this),
          max_batch_entries=max(entries)
