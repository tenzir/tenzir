// Quartile interpolation must not overflow for mixed-sign samples near the
// float64 limit, where the naive sample difference is infinite.
from {x: -1.0e308},
  {x: 1.0e308},
  {x: 1.2e308},
  {x: 1.4e308}
summarize skew=skewness(x, method="bowley")
skew = round(skew * 10000) / 10000
