// Each list row retains the configured quantile without sharing dynamic state.
// Previously, the second row degraded to quantile(q=0), the minimum.
from {xs: [1.0, 2.0, 3.0]}, {xs: [4.0, 5.0, 6.0]}
select q = xs.quantile(q=0.5)
