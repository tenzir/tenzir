// Regular list calls reset dynamic state between rows but keep configuration.
from {xs: [1.0, 2.0]}, {xs: [8.0, 9.0]}
select model = histogram(xs, bins=2, width=5.0)
select count=model.count, counts=model.bins.map(b => b.count)
