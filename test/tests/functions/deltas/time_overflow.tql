// Two valid timestamps can lie further apart than the signed duration
// representation allows (~292 years). The difference must become null with a
// warning instead of silently wrapping.
from {xs: [1900-01-01T00:00:00, 2262-01-01T00:00:00]}
xs = xs.deltas()
