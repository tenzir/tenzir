# Base image settings
VAST_CONTAINER_REGISTRY=docker.io
VAST_CONTAINER_REF=latest

# Changing the Quarto version might break compatibility with dependencies
QUARTO_VERSION=1.1.251

# Required host UID/GID when using Quarto in bind mode
HOST_UID=$(id -u) 
HOST_GID=$(id -g) 
