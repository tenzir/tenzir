title: "OCSF Casting & Recursion"
description: "This release introduces the `ocsf::cast` operator to streamline schema transformations for OCSF events and adds support for one-level recursion in OCSF objects, enabling recursive relations such as `process.parent_process` and `analytic.related_analytics`."
changes:
  - cancel-random-detail
  - crash-vital-highly
  - equal-closer-entry
  - forum-within-unlock
  - going-enemy-sixty
  - style-relax-period
