id: tenzir
name: Tenzir Node
description: >-
  The security data pipeline engine that collects security telemetry, normalizes
  events to OCSF, and offers an open data lake for storage.
repository: tenzir/tenzir
# PR references are optional: monorepo PR numbers no longer refer to the
# tenzir/tenzir repository, so new entries omit them. Entries released before
# the monorepo migration keep their PR metadata ('omit_pr: true' would reject
# it retroactively).
