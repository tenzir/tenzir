The `vast exec` command now supports implicit sources for pipelines that start
with events or bytes: `read json` and `load file -`, respectively. This
complements the already existing implicit sinks.
