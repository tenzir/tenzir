Several new options -- `data`, `json`, `form`, `skip_peer_verification`,
`skip_hostname_verification`, `chunked`, `multipart` -- are now available for
the `load_http` operator. The `skip_peer_verification` and
`skip_hostname_verification` are now also available for the `save_http`
operator.
